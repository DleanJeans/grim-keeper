import { StyleSheet } from 'react-native';

export const NOTE_REFERENCE_ICON_SCALE = 1.5;
export const NOTE_REFERENCE_ICON_SIZE = 16;

export const noteReferenceStyles = StyleSheet.create({
  container: {
    gap: 0,
    paddingHorizontal: 0,
  },
  text: {
    fontSize: 13,
    lineHeight: 18,
  },
});
